---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getApiBaseUrl } from '../../lib/api';

const apiBase = getApiBaseUrl();
---

<BaseLayout title="ekü" description="Eventos y cultura local cerca de ti.">
  <section class="page-hero page-hero--brand">
    <p class="eyebrow">Cultura local</p>
    <h1>ekü</h1>
    <p>Encontrá lo que está pasando cerca — y reservá tu lugar en un paso.</p>
  </section>

  {!apiBase && (
    <div class="alert alert--error" role="alert">
      Falta <code>PUBLIC_API_BASE_URL</code>. Copia <code>.env.example</code> → <code>.env</code> y reinicia el servidor.
    </div>
  )}

  <div id="status" class="alert alert--info" hidden></div>
  <div id="list" class="event-feed" aria-live="polite"></div>
</BaseLayout>

<script>
  import { clientApi, ClientApiError } from '../../lib/client-api';

  type EventItem = {
    id: string;
    name?: string;
    title?: string;
    startsAt?: string;
    startAt?: string;
    startDate?: string;
    place?: string;
    placeText?: string;
    location?: string;
    locationText?: string;
    venue?: string;
    isVirtual?: boolean;
    virtual?: boolean;
    visibility?: string;
  };

  function titleOf(e: EventItem) {
    return e.name || e.title || 'Evento sin título';
  }

  function placeOf(e: EventItem) {
    if (e.isVirtual || e.virtual) return 'Virtual';
    return e.place || e.placeText || e.locationText || e.location || e.venue || 'Lugar por confirmar';
  }

  function partsOf(e: EventItem) {
    const iso = e.startsAt || e.startAt || e.startDate;
    if (!iso) {
      return { month: '—', day: '?', time: '', full: 'Fecha por confirmar' };
    }
    const d = new Date(iso);
    if (Number.isNaN(d.getTime())) {
      return { month: '—', day: '?', time: '', full: iso };
    }
    return {
      month: new Intl.DateTimeFormat('es', { month: 'short' }).format(d).replace('.', ''),
      day: String(d.getDate()),
      time: new Intl.DateTimeFormat('es', { hour: '2-digit', minute: '2-digit' }).format(d),
      full: new Intl.DateTimeFormat('es', {
        weekday: 'short',
        day: 'numeric',
        month: 'short',
        hour: '2-digit',
        minute: '2-digit',
      }).format(d),
    };
  }

  const status = document.getElementById('status')!;
  const list = document.getElementById('list')!;

  function showStatus(msg: string, tone: 'error' | 'info' | 'ok' = 'info') {
    status.hidden = false;
    status.className = `alert alert--${tone}`;
    status.textContent = msg;
  }

  function escapeHtml(s: string) {
    return s
      .replaceAll('&', '&amp;')
      .replaceAll('<', '&lt;')
      .replaceAll('>', '&gt;')
      .replaceAll('"', '&quot;');
  }

  async function load() {
    showStatus('Buscando eventos…', 'info');
    try {
      const data = await clientApi<{ items?: EventItem[] } | EventItem[]>('/events');
      const items = Array.isArray(data) ? data : data.items || [];
      status.hidden = true;
      if (!items.length) {
        list.innerHTML =
          '<div class="empty"><p><strong>Todavía no hay eventos públicos.</strong></p><p class="muted">Cuando un organizador publique uno, aparece acá.</p><p style="margin-top:1rem"><a class="btn" href="/organizador">Ir a organizador</a></p></div>';
        return;
      }
      list.innerHTML = items
        .map((e, i) => {
          const when = partsOf(e);
          const badge =
            e.visibility && e.visibility !== 'public'
              ? `<span class="badge">${escapeHtml(e.visibility)}</span>`
              : '';
          return `<a class="event-row" href="/eventos/${encodeURIComponent(e.id)}" style="animation-delay:${Math.min(i, 8) * 35}ms">
            <div class="event-row__date" aria-label="${escapeHtml(when.full)}">
              <span class="month">${escapeHtml(when.month)}</span>
              <span class="day">${escapeHtml(when.day)}</span>
              <span class="time">${escapeHtml(when.time)}</span>
            </div>
            <div class="event-row__body">
              <h2>${escapeHtml(titleOf(e))}</h2>
              <div class="meta"><span>${escapeHtml(placeOf(e))}</span>${badge}</div>
            </div>
            <span class="event-row__go">Ver →</span>
          </a>`;
        })
        .join('');
    } catch (err) {
      const msg =
        err instanceof ClientApiError
          ? err.message
          : err instanceof Error
            ? err.message
            : 'Error desconocido';
      showStatus(`No se pudo cargar la lista: ${msg}`, 'error');
      list.innerHTML = '';
    }
  }

  load();
</script>
