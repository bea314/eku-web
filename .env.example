# Nest API base URL (no trailing slash, no /api path).
# Client and server join `/api/...` themselves.
# Example local Nest: http://localhost:3000
PUBLIC_API_BASE_URL=http://localhost:3000

# Optional server-only override (falls back to PUBLIC_API_BASE_URL).
# API_BASE_URL=http://localhost:3000
