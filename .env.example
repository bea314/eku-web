# Nest API base — Crop contract: ALREADY includes /api (no trailing slash after).
# Paths join as ${PUBLIC_API_BASE_URL}/events → http://localhost:3000/api/events
PUBLIC_API_BASE_URL=http://localhost:3000/api

# Optional server-only override (same shape, with /api).
# API_BASE_URL=http://localhost:3000/api
